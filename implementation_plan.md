# خطة تنفيذ واختبار لوحة تحكم الأدمن وتطوير الـ UX/UI (Implementation Plan)

هدف هذه الخطة المحدثة هو تطوير وتحديث لوحة تحكم الأدمن (Admin Panel) بتصميم عصري وأنيق مع شريط جانبي (Sidebar Navigation)، ونظام ترقيم صفحات (Pagination) لسلاسة الاستخدام، وتأمين وحفظ ملفات Gerber وتنزيلها مع ضمان ثبات وحساب التسعير بدقة متناهية دون أي خلل، مع الحفاظ الكامل على البيانات والتكامل التام مع الإيميل والواتساب ورفع الكود في النهاية على GitHub.

---

## 🎨 التغييرات والتحديثات الجديدة المطلوبة

### 1. إعادة تصميم واجهة الأدمن بأحدث معايير الـ UX/UI (Sidebar Navigation)
- **الشريط الجانبي (Sidebar)**: تحويل التبويبات العلوية إلى قائمة جانبية ثابتة وأنيقة تضم الأيقونات والمحتوى بشكل منظم ومريح للعين.
- **التنقل السلس (Smooth Navigation)**: تجربة مستخدم سريعة وبسيطة للانتقال بين أجزاء لوحة التحكم بدون إعادة تحميل.
- **شريط علوي (Header)**: يعرض اسم الأدمن، زر معاينة الموقع، وزر تسجيل الخروج.

### 2. إضافة نظام ترقيم الصفحات (Pagination)
- **الترقيم بالأرقام (1, 2, 3...)**: إضافة نظام ترقيم ديناميكي في أسفل القوائم (المشاريع، القطع الميكانيكية، طلبات PCB، 3D، Stencil، التخرج، والرسائل).
- **التحكم في عدد العناصر**: عرض 6 عناصر فقط في الصفحة الواحدة لتسريع التصفح وسهولة المتابعة مع أزرار (التالي / السابق / أرقام الصفحات).

### 3. حل مشكلة ملف الجاربر (Gerber File) وتنزيله
- **ثبات التسعير (Price Calculation Fix)**: ضمان تطابق التسعير تماماً سواء تم إرفاق ملف Gerber أم لا (تحويل كافة الحقول إلى قيم رقمية صريحة وتوفير Fallback للملفات المحلية في حالة عدم الاتصال بـ Supabase حتى لا يفشل الطلب).
- **تنزيل الملف للأدمن (Gerber Download)**: إضافة زر تنزيل مباشر `تحميل ملف Gerber` داخل كارت طلب PCB و Stencil في لوحة تحكم الأدمن حتى يستطيع الأدمن تحميله بنقرة واحدة.

### 4. الربط المباشر مع صفحات الموقع الأصلية
- انعكاس كافة البيانات المعدلة في الأدمن فوراً على صفحات الموقع (معرض المشاريع، حاسبات الأسعار، كروت القطع الميكانيكية، وسائط السلايدر، ومواعيد دعم التخرج).

### 5. إشعارات البريد والواتساب
- الحفاظ التام واختبار استدعاء البريد الإلكتروني وتوليد روابط الواتساب التلقائية لكافة أنواع الطلبات.

### 6. الرفع على GitHub (الخطوة الأخيرة)
- إنشاء مستودع جديد على حسابك في GitHub ورفع النسخة النهائية بعد تجربة واختبار كافة الميزات بنجاح.

---

## 🧪 خطة التنفيذ والاختبار المحدثة (Action Steps Status)

### الخطوة 1: تحديث الـ HTML & CSS للوحة التحكم (Sidebar + Pagination + Toasts)
- [x] تعديل [admin.html](file:///d:/reverse%20tech/01%20projects/website/reverse-tech-final-social-newsletter-email%20%282%29/alieem-reverse-tech-final-social-newsletter-email/public/admin.html) لبناء الهيكل الجديد (Sidebar + Main Content Header + Pagination Container).
- [x] تحديث [admin.css](file:///d:/reverse%20tech/01%20projects/website/reverse-tech-final-social-newsletter-email%20%282%29/alieem-reverse-tech-final-social-newsletter-email/public/admin.css) بتنسيقات Sidebar زجاجية ومودرن مع تحسين التجاوب للهواتف والأجهزة المختلفة.

### الخطوة 2: تحديث الـ JavaScript ونظام الترقيم (admin-v2.js)
- [x] إضافة دالة الترقيم العامة `renderPaginationHTML(totalItems, currentPage, itemsPerPage)`.
- [x] ربط نظام الترقيم بـ (المشاريع، القطع الميكانيكية، وكافة الطلبات والرسائل).
- [x] تحسين دالة عرض كروت طلبات PCB لإضافة زر تنزيل ملف Gerber مباشر للأدمن `downloadGerberFile`.

### الخطوة 3: إصلاح مسارات Gerber والباك اند (server.js)
- [x] التأكد من معالجة حقول طلبات PCB برمجياً بحيث لا يؤثر وجود الملف إطلاقاً على دالة `calculatePcbPrice`.
- [x] إضافة حفظ محلي لملفات Gerber في حالة عدم توفر Supabase وتوفير مسار `/api/pcb-orders/:id/gerber` لتنزيلها بنقرة واحدة.

### الخطوة 4: التنسيق والتأكد من العمل الميداني
- [x] اختبار كافة التبويبات والأزرار والترقيم بالأرقام.
- [x] التأكد من وصول البريد الإلكتروني وروابط الواتساب.
- [x] التأكد من انعكاس التغييرات على صفحات الموقع الرئيسية.

### الخطوة 5: الرفع على GitHub
- [x] ربط المستودع `https://github.com/AbdelalieemMena/reverse-tech-website.git` ورفع الكود كاملاً لفرع `main`.
